# Client-visible (Vite) environment variables
# These are exposed to the browser at build time.

# Public site metadata (optional)
VITE_SITE_URL=https://gotchicloset.xyz

# Subgraph endpoint (optional; defaults to public Goldsky)
VITE_GOTCHI_SUBGRAPH_URL=

# Base RPC used by wallet connectors (optional)
VITE_BASE_RPC_URL=https://mainnet.base.org

# WalletConnect project ID (required for wallet connect)
VITE_WALLETCONNECT_PROJECT_ID=

# Donation address for the homepage (optional)
VITE_DONATION_ADDRESS=

# Partner attribution (optional)
VITE_GRIMLABS_NAME=GrimLabs
VITE_GRIMLABS_URL=https://grimlabs.xyz

# Dev-only: override Vite /api proxy target (optional)
# Example for vercel dev: http://localhost:3000
VITE_API_PROXY_URL=

# Dev-only: override allowed CORS origins for local API server
# Comma-separated list. If empty, defaults to 5173/5174/3000.
VITE_DEV_ALLOWED_ORIGINS=

# Serverless / Node-only environment variables
# These are read by Vercel functions (process.env.*)

# SVG service contract address (optional; defaults to Aavegotchi diamond)
VITE_GOTCHI_DIAMOND_ADDRESS=0xA99c4B08201F2913Db8D28e71d020c4298F29dBF