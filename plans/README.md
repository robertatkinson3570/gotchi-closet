# Implementation Plans

Generated by the `improve` advisor on 2026-06-19 (against commit `192d483`). Each plan is self-contained for an executor with zero prior context. Read the plan fully before starting, honor its STOP conditions, and update your row below when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Cut DaoPage/initial bundles via code-splitting | P2 | M | — | DONE (DaoPage 403→6.1 KB gzip; data-wearables + SnapshotVotePanel chunks split out) |
| 002 | Backend auth & payment hardening | P1 | M | — | TODO |
| 003 | Lending characterization tests | P1 | M | — (pairs with 002) | TODO |
| 004 | Roadmap (direction options) | P3 | — | — | INFO (not executable) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale).

## Recommended order
Do **003** and **002** first (both P1; 003 is low-risk tests that also de-risk 002's payment change). **001** (perf) is independent and can run in parallel. **004** is a planning input — turn its starred items into spike plans 005/006 on request.

## Dependency notes
- 003 ↔ 002: independent, but if 002 lands first, 003's `verifyPayment.test.ts` should also assert the new confirmation-depth rejection (noted in both plans).
- 001 is fully independent (build config + import timing only).

## Scope of the audit
Audited at `standard` effort across four areas the maintainer requested: bundle/perf (frontend build), backend security (auth/payment/rate-limit), backend test coverage (companion/soul/lending/roast), and product direction. **Not audited:** dependency CVEs (`npm audit` not run), frontend test coverage, CI/deploy config, the Aavegotchi contract ABIs themselves, and non-lending route handlers (flagged as a future integration-test plan).

## Findings considered and rejected (do not re-audit)
- **SEC-09 "/premium/claim wallet spoofing → credit theft"** — REJECTED. Verified in `server/routes/companion.ts:135-145`: `verifyGhstPayment` requires `expectedFrom == wallet` and `addCredits` credits that same payer wallet idempotently by `txHash`. A front-runner credits the rightful payer, not themselves. Not exploitable; no signature needed on `/premium/claim`.
- **Premature credit burn in /chat** — REJECTED as a security issue. `burnCredit` only runs when the premium LLM returns non-null (`companion.ts:88-91`); a failed premium call falls through to free with no burn. Working as designed.
- **SQL injection** — none found; `better-sqlite3` prepared statements used throughout.
- **Perf LOW-confidence items** (lucide-react tree-shaking, html-to-image split, viem treasury defer) — not planned; need a bundle-visualizer measurement first. Add `rollup-plugin-visualizer` and re-measure before investing.
- **SEC-08 dev CORS origins (`*.replit.dev`)** and **SEC-07 attestor-key error-log hygiene** — minor defense-in-depth; fold into 002 only if cheap, otherwise skip.

## How these were produced
Parallel read-only audits per area, then every finding that became a plan was re-verified by reading the cited code directly (subagent line numbers were treated as leads, not facts). No source code was modified by the advisor; these plans are the only output.
